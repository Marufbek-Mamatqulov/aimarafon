# Example: run this project next to existing Moodle installation
# Replace YOUR_DOMAIN and SSL paths before enabling.

server {
    listen 80;
    server_name ai.YOUR_DOMAIN;

    location / {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# Optional SSL block (after certbot)
# server {
#     listen 443 ssl http2;
#     server_name ai.YOUR_DOMAIN;
#
#     ssl_certificate /etc/letsencrypt/live/ai.YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/ai.YOUR_DOMAIN/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:4000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
